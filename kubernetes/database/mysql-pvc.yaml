apiVersion: v1
kind: PersistentVolumeClaim
metadata:
  name: mysql-pvc
  namespace: myelga
  labels:
    app: mysql
spec:
  storageClassName: microk8s-hostpath
  resources:
    requests:
      storage: 5Gi
  accessModes:
    - ReadWriteOnce